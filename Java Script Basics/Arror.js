let add = (a,b) => 
{var s; s=a+b; return s;}
console.log(add(15,10));

//Here if there is only one statment with no braces, it returns withput the keyword "return"
//If multiple statemnts uses braces, so we need to use return keyword